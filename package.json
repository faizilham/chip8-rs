{
  "name": "chip-rs-web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "rimraf dist && parcel index.html",
    "build": "rimraf dist && parcel build index.html"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "rimraf": "^3.0.0"
  }
}
